local lightsOrderCli = {} local WCServer function lightsOrderCli:set() WCServer = self end  lightsOrderCli.finalize = function() local co = require 'component'; local os = require 'os'; local thread = require 'thread'; return thread.create(function()   WCServer.execAlias('lights',0); end) end lightsOrderCli.run = function() local co = require 'component'; local os = require 'os'; local thread = require 'thread';	local event = require 'event'; return thread.create(function()  for i=0, 10 do  WCServer.eAlias(_, _, _, _, _, _, 'lights',255); os.sleep(2);  WCServer.eAlias(_, _, _, _, _, _, 'lights',0);  end   WCServer.execAlias('lights',0);  os.sleep(1); WCServer.threadEnded('lightsOrderCli') end) end return lightsOrderCli