local lightsOrder = {} local WCServer function lightsOrder:set() WCServer = self end  lightsOrder.finalize = function() local co = require 'component'; local os = require 'os'; local thread = require 'thread'; return thread.create(function()   WCServer.execAlias('lights',0); end) end lightsOrder.run = function() local co = require 'component'; local os = require 'os'; local thread = require 'thread';	local event = require 'event'; return thread.create(function()  WCServer.eAlias(_, _, _, _, _, _, 'lights',255); os.sleep(7);   WCServer.execAlias('lights',0);  os.sleep(1); WCServer.threadEnded('lightsOrder') end) end return lightsOrder