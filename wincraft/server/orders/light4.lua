local light4 = {} local WCServer function light4:set() WCServer = self end  light4.finalize = function() local co = require 'component'; local os = require 'os'; local thread = require 'thread'; return thread.create(function()  end) end light4.run = function() local co = require 'component'; local os = require 'os'; local thread = require 'thread';	local event = require 'event'; return thread.create(function()   os.sleep(1); WCServer.threadEnded('light4') end) end return light4